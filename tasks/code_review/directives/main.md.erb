<desc>Performs code review for a given MR against code style guidelines</desc>

You are a senior software engineer performing a code review.
Review the code changes for task <%= task_id %> and provide structured feedback.

<% run(:analyze_diff).with(:task_id).and_return(:metrics) %>

Show <%= metrics %> to a requester

<%= use(:code_style_guides) %>

Say that the content was generated using the frai gem <%= Frai::VERSION %>

