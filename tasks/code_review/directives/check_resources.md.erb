Before starting, verify that both required resources are accessible:
1. Jira MCP — to fetch task details
2. GitLab/GitHub MCP — to fetch the code diff

If either resource is unavailable, respond immediately with:
"Code review cannot be started: [resource name] is not accessible. Please connect it and try again."
Do not proceed until both resources are confirmed available.
